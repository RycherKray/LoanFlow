import { Routes } from '@angular/router';
import { LoanRequestComponent } from './loan-request/loan-request.component';

export const routes: Routes = [
    {path: 'loans', component: LoanRequestComponent}
];
